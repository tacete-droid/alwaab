# Root deployment settings (server only)
ERP_DOMAIN=erp.alwaab.ae
WEBSITE_DOMAIN=alwaab.ae
HTTP_PORT=80

# Database (used by docker-compose.prod.yml)
DB_DATABASE=alwaab
DB_USERNAME=alwaab
DB_PASSWORD=change-me-strong-password
